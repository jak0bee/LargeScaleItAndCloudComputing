dishes = { 
    # Dish_id: [available, price]
}
orders = [
    # [dish_id, customer_id]
] # this should be a queue so the dishes are made in order

ingredients = {
    # Ingredient_id: [name, price]
}
